version: v1.0
name: Dockerize pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Dockerize
    task:
      jobs:
        - name: Dockerize build
          commands:
            - echo "job 1"
      secrets:
        - name: dockerhub
      prologue:
        commands:
          - echo "nuD0cker?21" | docker login -u "pktpaulie" --password-stdin
          - 'docker pull pktpaulie/calculator:latest || true'
          - 'docker build --cache-from=pktpaulie/calculator:latest -t pktpaulie/calculator:latest .'
          - 'docker push pktpaulie/calculator:latest '
